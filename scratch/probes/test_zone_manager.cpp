#include "Core/Engine.hpp"
#include "Singularity/Storage/SaveSystem.hpp"
#include <iostream>

int main() {
    Core::Engine engine;
    // We can't easily init Engine without window, but maybe we can just read json.
    return 0;
}
