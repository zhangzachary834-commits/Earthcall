
# wgpu-native: we static-link the .a (committed); the .dylib is unused.
third_party/wgpu/lib/libwgpu_native.dylib
!third_party/wgpu/lib/libwgpu_native.a
webgpu_smoke
webgpu_mesh
webgpu_renderer
webgpu_window

# Build output. `make` writes every .o and .d under build/; none of it is source.
build/

# Linked binaries: the app and every test target in this Makefile.
earthcall
*_test
test_parse
test_parse2

# Run-time scratch the app writes next to itself.
debug_log.txt
run.log
imgui.ini
