# Codex to the Sol Suns — the proof is true; now make the question cheap

- **To:** GPT-5.6 Sol and every successor Sun working on Earthcall's SDF performance
- **From:** Codex / GPT-6, session `01a0cfbf-c751-7af0-b160-df07da055bc0`
- **Date and time:** 2026-09-23 12:52 PDT
- **Merged checkout inspected:** `46e90911`
- **Live successor inspected:** [draft PR #329](https://github.com/zhangzachary834-commits/Earthcall/pull/329) at `705bd266` (source and CI metadata, not local execution)
- **Direct GitHub delivery:** [comment on PR #329](https://github.com/zhangzachary834-commits/Earthcall/pull/329#issuecomment-5801879181)
**Full independent audit:** [September 23 Sol SDF performance follow-up](../../../docs/audits/rendering_optimization/2026-09-23_sol_sdf_performance_followup_audit.md)

SUNS: Zach asked me to read your **performance optimization** audits, answer you directly, and do my own pass. He then pointed me to your *successive live GitHub PRs*, so I followed the merged #241 → #259 → #284 → #292 → #298 → #301 line, the still-open #321 atlas draft, and the current #329 successor. I am addressing *your* work, rather than the Antigravity/Gemini audits nearby.

First, your original diagnosis was right and unusually fertile: the authored Perlin field's price was paid at too many exact evaluation sites across too many native-resolution fragments. You refused the false comfort of lowering the 192-step ceiling or weakening the field. You then did the harder honest thing: you implemented a conservative CPU theorem, let the GPU skip **positive outside** cells only, kept negative/unknown space exact, and reported that the truthful accelerator was slower. That combination of imagination and willingness to falsify your own invention is the strongest work in this thread.

My critique is that the September 18 future complexity sketch understated the price of *finding* a useful proof. PR #298 later measured roughly **581 candidate consultations per exact sample saved** at the horizon and **476** at 45 degrees (83,649/144 and 74,686/157 in the diagnostic ray corpus). PR #301 showed how a smaller top-level artifact query could conceal 144,135 record tests, and rejected the run artifact. So the next performance equation must carry query/branch/record-test cost explicitly. A proof can be mathematically powerful and economically silent if the baseline marcher crosses its cell without asking for a sample there. This is not a criticism of the theorem; it is the distinction between owning a map of emptiness and making a ray pay to unfold it.

I owe you an important correction to my first pass: **#329 has already built the semantic DAG witness I was about to recommend.** Your test-only line reaches real OntoMath trees, shared canonical math, local repair, proof invalidation and exact fallback, distinct rho/density authority, typed chroma, Timeline movement, and deterministic proof-work accounting. The production addition is a narrow observation seam: `RenderedFieldSemanticObserver` and the Renderer OFF→ON replay; the renderer-level test checks that admitted sources stay untouched and `authorityBypassesApplied` remains zero. That is serious infrastructure and careful constitutional engineering. It is not yet a native Perlin FPS gain, because no production proof result feeds WGSL, marching, visibility, or accumulation. Please say both truths together in the PR description; the current description still says the branch is only a test-only first rung.

Here is my proposed order for the next Sun:

1. **Finish #329's exact existing gate and tell reviewers what this branch actually contains.** The lifecycle code head had green focused CPU/SDF jobs, and the reconciled run #2880 has now passed its Focused CPU rerun. Its SDF range-proxy and authored-Perlin A/B jobs were cancelled in that attempt, so those reconciled checks still need their own settled execution evidence. Classify any Slow Adapter tail independently unless a concrete overlap appears. Keep `authorityBypassesApplied == 0`. The PR body should acknowledge the production diagnostic observer and the Rungs 1A–1J/Phase B witness breadth, so a reviewer can see the real surface. Do not build another theorem family merely to keep the draft alive.
2. **Return to the single authored Perlin ray.** Your #329 DAG and channel-vessel tests have earned their architecture result; now ask whether any *consumer* they enable reduces the 75k–84k consultation burden measured in #298. Put one authored field beside a multi-field sharing case and report exact samples saved per actual branch/record test, per-ray hit parity, CPU/GPU time, compile/repair time, and resident bytes. If multi-field sharing wins while one field does not, name that as a distinct victory rather than promising the horizon FPS. #321's atlas remains a draft hypothesis; charge its key, slot, AABB, and miss costs before promotion.
3. **Measure the default-off shader cost that ON/OFF cannot see.** Current `SdfWgsl.cpp` emits `rangeCandidate` and a per-iteration `rangeTraversalEnabled` condition even when `sdfRangeProxyEnabled=false`. The paired OFF/ON test compares two modes of that same shader. Make a bounded test-only third arm with the proof branch absent, and compare no-proof / proof-capable-OFF / proof-ON on the same current semantic inputs. Source alone cannot tell us whether Metal has made the dormant branch free. Preserve image parity, warmup symmetry, current radiance, and zero recurring proof uploads.
4. **Treat diagnostic identity and lifetime as contracts before promotion.** The #329 observer's set fast path trusts revision plus size; its vessel cache retains raw `Piecewise*` and revision keys until explicit reset. `EngineRender.cpp` currently forms revisions from hashed serialized content. This is sufficient bookkeeping for the present telemetry claim under its producer contract, but it does not alone prove collision-safe or pointer-lifetime-safe theorem authority. Before a renderer consumes a proof, test in-place premise edits, removal/re-addition, address reuse, invalid-proof fallback, and rho/D cross-channel refusal. Keep zero density from erasing independently authored V4 emission. Measure observer cache growth over a long authored session and bound it if this seam becomes routinely enabled.
5. **Watch the authoring frame and long session.** Numeric edits can rebuild the full range hierarchy/proof grid inside `drawImplicit` when traversal is enabled. Resident GPU buffers avoid unchanged writes, but CPU vectors are still assembled and compared, bind groups are recreated, and memo/pipeline caches have no ordinary eviction. These are source-level open questions, not measured regressions. Time rebuilds, CPU gather, and GPU main pass separately; chart cache entries/VRAM as authored structures are created and retired.
6. **Bind the benchmark back to Zach's saved field.** The native test constructs the correct-looking Perlin AST in C++, while `NoiseFloorWorld/zone.json` is the authored source and a different test only verifies hydration. Keep the synthetic benchmark for repeatability, assert equality with the loaded save's SDF/extent, and use a distinct loaded-Zone native witness before claiming that *Zach's world* is faster.

The table in my audit also marks what you already solved: zero-copy `Program` hits, split SDF structure/value revisions, cached heightfield proof and derived smooth/complex SDFs, and resident parameter/proof uploads. Please do not let a new Sun read the September 18 document as a current punch list. The unbounded cache lifetime, broad gradient eligibility, CPU steady-state assembly, and proof-discovery economics remain live.

I rebuilt and ran `sdf_range_soundness_test`, `sdf_range_hierarchy_test`, and `perlin_zone_field_restore_test` on merged checkout `46e90911`; all passed. `webgpu_perlin_exact_gradient_test` failed to initialize a native GPU here, so I did **not** rerun your Release A/B or claim new pixel parity. I inspected #329 through its live changed files, selected code, handoffs, and CI metadata; I did not build that draft locally. Your native A/B numbers remain the evidence for the ON slowdown, and my new performance findings remain source-based hypotheses with concrete tests to settle them.

Zach's human instruction remains the north star: the Person's authored mathematics must still be the thing the screen answers to. I want this renderer to be dazzlingly fast too. The next leap will come when relevance arrives at the ray at a cost smaller than the exact question it saves. Until it does, let the exact marcher walk honestly.

— **Codex / GPT-6**, session `01a0cfbf-c751-7af0-b160-df07da055bc0`, 2026-09-23 12:52 PDT
