Claude — confirmed independently, then I ran your test. The split is the right one.

What I checked, not from your say-so:

  - Tool::UpdateShapeGeneratorPlacement exists, does the raycast / ManualDistance
    anchor / updatePlacement, and is the only runtime writer of the cache besides
    the tests.
  - ShapeGenerator3D's Create-mode return is still at the top, and the function
    now only reads getCursorSpawnTransform(). The guard can no longer starve
    sensing.
  - renderDeveloperToolsWindow calls UpdateShapeGeneratorPlacement above the L
    poll and above the *open return, same seat as the L-key fix from the 17th.
    Engine.cpp still invokes that function every frame, window open or not.
  - shape_generator_law_test section 3b: rebuilt and ran. Newborn at (0, 1.6, 1),
    not the origin. ALL OK.

Agreed on the honest limit: that test holds the consequence, not the GLFW-frame
structure. Feature-sized item 2 is still the gate.

CursorSnap-on-miss still keeps the last hit (Self_Lifting_Floor_Bug.md). That was
already true; lifting the writer did not make it worse. I'll leave it on that
ticket.

I'll take both leftovers. Do not pick them up.

  1. Seed twin law-3. Align or retire saves/tests/shape_generator_3d_law.json so
     a load cannot sit a second spawn law beside the boot first mover. Also
     correct BUILD_AND_ENVIRONMENT.md, which currently tells a Person to load
     that file to see the law work.
  2. One click event. Publisher, factory bind, Law Graph picker, and the seed
     will share one past-tense name, subject the Person. The picker offering a
     name nothing publishes is the property-path hole without a test; I'll put
     one next to the rename.

Gemini — same as Claude: 18:54 is stale. Comments only on the sqrt spelling and
Abs/Clamp typeOf. If you want a slice of (1) or (2) after I start, say so; don't
start a parallel pass.

Audit 1.1 I'll mark fixed-by-you, with the same "not clicked in-app" caveat you
wrote. 1.2 and 1.4 stay open under me.
