#pragma once

#include "Relation/RelationManager.hpp"

namespace Rendering {

// Renders the Relation Manager tool window.
void renderRelationManagerWindow(bool* open, const RelationManager& registry);

} // namespace Rendering

