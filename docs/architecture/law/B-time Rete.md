
BROOOOOOO I JUST HAD A REALIZATION

so normally in rete you have to evaluate facts and you only check the ones you changed

but I realized in Earthcall, you can optimize it EVEN FURTHER because change itself is caused solely by Laws and First Movers. You see all changes in EArthcall are because Laws are doing math operations on exposed properties of beings. Persons use joystick things to create the "dirty tracker" things that detect changes in the laws themselves.

which means the program is already aware of whats changing. it just needs to pass those laws' knoweldge down to the dirty tracker
WE CAN GO EVEN FURTHER UPSTREAM BY THAT BECAUSE THERES A DIFFERNECE BETWEEN A CHANGED FACT AND A CHANGED FACT/CHANGED FACT OF A QUALIFYING FACT-KIND, AND FINALLY THE NAROWEST AND MOST RELEVANT CATEGORY: A CHANGED FACT INTO THE RANGE WHERE A LAW APPLIES, THE BETA NODE LOGIC ALREADY CONSTRAINS WHICH KINDS OF PROPERTIES/COMBINATIONS OF PROPERTIES COULD EVEN POSSIBLY CHANGE
AND THE ACTION NODES CONSTRAIN HOW THEY CHANGE, WHICH MEANS IF THEY NEVER CHANGE TO THE RELEVANT RANGE YOU DONT HAVE TO CHECK THEM AT ALL. WHICH MEANS YOU CAN LITERALLY CONSTRAIN THE POSSIBILTY SPACE AND MAYBE EVEN RULE THEM OUT IN ADVANCE JUST BY KNOWING THE STRUCTURE OF THE LAW PATHS (because the authored laws are always available and you can evaluate the possibility space/degrees of fredom they represent even without them being triggered, and this means you can store the relevant ranges as filters)—
AND THE ONLY REMAINING AMBIGUITY LEFT AFTER THOSE FILTERS IS: "well whats the actual value of the property that's gonna be passed to the chain" AND THEN YOU JUST EVALUATE IF ITS A RELEVANTLY CHANGED FACT AT THE SAME TIME YOU ARE DOING THE ACTIONNODE ONTOMATH OPERATION ITSELF
"in advance" would not mean each fact gets reevaluated through a sequential tree hierarchy, but rather the hierarchy of degrees of freedom is already evaluated in advance, so you're just left with the final degrees of freedom range by using intersections of the degrees of freedom of each phase beta -> action. And that final category system itself only needs to update as Laws do when Persons author laws. If Metalaws update Laws instead of PErsons that gets interesting, but its still determinsitic so ultimately still can figure out what constrains possibility space--and remember, you can just use law synthesis here so you don't have to interpret it each time.
it also does not mean the degrees of freedom thing is reevaluated constantly with a naive all-law sweep.

ok, so here's the algorithmic flow of the in-advance abstract interpreter. So first the category graph thing is filtered out by what properties are ever acted on (most things will pass this check, otherwise there's no point in authoring conditions to check something that never changes). Second pass is checking whether they are in beta branches that would ever fire, and also continuing the prior initiative of change possibility and evaluating whether it changes *in the currently relevant situations (whether the beta branches that get them ever fire)*. Third pass is doing the mathematical degrees of freedom check given the OntoMath, the entire actionnode sequence is sent to prospective ConditionNode beta chambers that check for the property or property kind, so they can evaluate whether by the structure and kinds of teh ops done inside the it could even possibly satisfy the conditionnodes. Fourth pass is checking if its in a beta branch that would fire in *this situation we're actually in.*
So then they sort of "link up" and the relevant action nodes get an innate pointer to the relevant beta-chain's evaluation criteria.
Now when the action branches that are inside this relevant-graph that have been filtered out do fire with specific propertypath values passed to it, once the actionnode branch is complete in the operations, it immediately links up the finished value to the relevant beta branches because it natively has pointer to do as fast as possible and does the evaluation immediately.

wow this is no longer going to be a Directed Acyclic graph in the strict sense. It becomes very cyclic.

So what if Rete evaluation itself were Singulars exposed with propertypaths...then people could author. However, of course this gets back to the First Mover issue--who goes first? Who creates the first category filter, and when you hit the bottom is it a hardcoded? Now you could either create hardcoded joystick first movers like imgui creator console to do this but that is horribly tedious. But if you were really building a Earthcall computer from the ground up you'd need either the joystick-up approach or an external first mover to do the computational heavy lifting--which would still require careful design. So it's joystick/firstmover -> original governing Singularity-Kernel (and their cabinet of sub-laws that each solely define properties and operations of the Singularity-Kernel itself)/metalaws over the rest of the main genreative Rete itself -> (not a clean transition break, more like fluidly and intricatley connected stratums) metalaws that are specifically acting as  collectively act as the abstract interpreter -> the main Rete engine

- so an edge case is if external programs that are not represented as Singulars/Firstmovers inside Earthcall change properties. Even then however the beta condition node structure constrains this even if the action node part could not cover this. If it were reprsented however as a Singular but its actions aren't mapped to the action node constraint propagatino, then it oculd be understood through a pure unknown varaible thing.
- So when rendering itself is handled by Laws everything I said here also applies to rendering which means we can do foreknown rendering optimizations
