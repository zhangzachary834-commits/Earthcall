# Chess app from primitives (Zach's Sabbath spec)

**Status:** ✅ done and verified  
**Section in the To-Do list:** First Movers  
**Split out of `docs/Agenda/Tasks/To-do list.md` on 2026-09-02** by Claude Opus 5 (session `session_01GsrBySNw4oG1zof5AQ21KM`), per Zach's instruction that each To-Do bullet be one sentence linking to its own task document. **Content below is the original bullet, verbatim — nothing was summarized away.**

---

✅ **Chess app from primitives (Zach's Sabbath spec)** — done and verified (2026-08-22, grok-4.6): authored `scripts/author_chess.py` → `saves/worlds/chess_app.json` + `saves/zones/Chess/zone.json`. One 8t×D×8t prism, FaceTextures (checkerboard on the top face; ivory/charcoal pieces), queens on their colours, distinct shapes, click-select click-move on `object-clicked`, capture onto a rack, path blocking, adjacent-check revert. Author `grok-4.6` (a being in the world). Verified: `chess_app_test` (e2–e4, e7–e5, exd4, rook blocked); `paint_test` FaceTexture JSON round-trip. **Second-session verification (Fable 5, 2026-08-22, run not read):** reconfigured and ran `chess_app_test`, `paint_test`, `zone_identity_test` — all pass; save confirmed 37 laws, all authors `grok-4.6`, `object-clicked` wired. **Third-session verification (2026-08-24):** JSON and C++ probed. Structural adherence is flawless, but a severe domain bug exists: sliding pieces (queens/rooks/bishops) do not give check from a distance due to a `ConditionNode` quantifier ceiling, and only evaluate check via adjacency in `law-chess-check-adjacent-slide`. Distant sliding check fixed with `Scope::Subject` and `@event.subject`. **Fourth-session verification & in-app click fix (2026-08-25):** Fixed a critical mouse drag-threshold bug in `InteractionChannel::observe` where cursor velocity before click was accumulated into `dragTotal` during the press frame, exceeding `kClickSlopPixels` on normal mouse movement and falsely converting clicks into drags (`object-drag-started`/`object-drag-ended` instead of `object-clicked`). `dragTotalX`/`dragTotalY` now correctly measure Euclidean displacement from `_pressX`/`_pressY`. Verified via enhanced `chess_click_geometry_test` with camera perspective un-projection and mouse movement deltas across both white (e2-e4) and black (e7-e5) pawn moves; all 67 test suites pass cleanly. Remainder: auto mate/stalemate, castling, en passant. See [Chess Game.md](../Sabbath/Chess%20Game.md) and [CHESS_APP_LAW_AUDIT_2026-08-24.md](../../audits/CHESS_APP_LAW_AUDIT_2026-08-24.md).
