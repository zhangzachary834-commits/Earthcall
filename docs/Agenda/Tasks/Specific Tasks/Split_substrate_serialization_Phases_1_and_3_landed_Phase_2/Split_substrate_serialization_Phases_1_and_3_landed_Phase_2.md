# Split-substrate serialization: Phases 1 and 3 landed, Phase 2 and most of Phase 4 did not — and the plan doc says otherwise

**Status:** open  
**Section in the To-Do list:** Essential Singularity substrate  
**Split out of `docs/Agenda/Tasks/To-do list.md` on 2026-09-02** by Claude Opus 5 (session `session_01GsrBySNw4oG1zof5AQ21KM`), per Zach's instruction that each To-Do bullet be one sentence linking to its own task document. **Content below is the original bullet, verbatim — nothing was summarized away.**

---

**Split-substrate serialization: Phases 1 and 3 landed, Phase 2 and most of Phase 4 did not — and the plan doc says otherwise.** (2026-09-02, measured; from [The Week the Chorus Became a Queue](../../Reflections%20on%20Earthcall%27s%20Progression/Reflections%20on%20Trajectory/The_Week_The_Chorus_Became_A_Queue.md) §4.) `writeMatterData` exists, `ZoneManager` hydrates in two steps, and `BinaryPack.hpp/.cpp` are gone from `src/Singularity/Storage/` — those parts of [SPLIT_SUBSTRATE_SERIALIZATION_PLAN_2026-09-01.md](../../plans/SPLIT_SUBSTRATE_SERIALIZATION_PLAN_2026-09-01.md) are real. But `saves/worlds/my_world.ecform` (93,572,409 bytes) is **byte-identical to `my_world.json`** (`cmp` reports no difference), holds 870 `"faceTextures"` and 635 `"faceColors"` keys, and is the largest file in the repository; `my_world.ecmatter` (18.2 MB) and `my_world.ecsave` (472 KB) sit alongside it. The triple-write Phase 4 promised to sunset is currently a **quadruple**-write: one world, four files, 112 MB. **Do:** (1) strip `faceTextures`/`faceColors`/`patch`/`polyhedron` out of the `.ecform` text path (Phase 2); (2) stop writing the monolithic `.json` and retire the `.ecsave` mirror (Phase 4); (3) until (1) and (2) land, correct the plan document — it currently claims the split "cures the JSON bloat." This is the save system, which the CRITICAL line at the top of this file is about; four writers of one truth is four places for them to disagree.
