Yeah so formations of Lexeme with Laws and basic opcode-like command roots that bootstrap them.

its literally Formations of Lexeme because everything you type in the Terminal is either a Lexeme or a symbol-Property of a Lexeme
Termainl displays lexeme also this can create beautiful termianl art

PLZ ROBOT GUY HALP

HAHAHAHA IM GONNA TRAP THE ROBOT GUYS IN THE TERMINAL OH WAI TTHEYRE ALREADY THERE

- Zach 

## Okay fine I'm gonna actually write the spec

Alright so the Terminal is our best test for these things: 
Fundamental first mover opcodes, Singulars and their Properties, 
First Movers enabling Lexeme-to-opcode transformation, and Law-governed Lexemes, and recursive Lexeme Formations, 
Lexemes wrapping opcodes as properties. Hierachy of Joys thus tests these. 

The Earthcall CLI should become a place where you can literally speak anything into existence. 

What I mean is this

a First Mover comamnd would be a opcode written as a Lexeme under a Law that wraps it. That command, for example, 
would be to author a Singular via Singular set to set Creation. 

Suppose I have a setsetcreation command. It should have something like this:
set<Singular set 1>, set<Singular set 2>, create Singular <Kind: base Singular (just a default Singular that's not 
specified), Object, Relation, Law, Formation, Lexeme, Zone, Home, Event, Moment, Material, etc.>, then it displays 
what properties the Singular should have.

Once you enter that, it lets you write OntoMath opcodes over the collective properties of Singular sets 1 and 2, 
and add or remove new properties. 

One such property is OntoMath opcodes.

The current CLI implements what is essentially a new subsystem above the essential ontology of creation. That's NOT how it should be. 
It should correspond to the invariatns the ontology already gives rather than hardcoding a command system sitting above it wearing an Earthcall costume.  

Person-facing First movers wrapping Opcodes around Lexemes. It says something like "." enable the first Singulars and Laws to be created 
-> 